const arr = ['btc', 'eth', 'dash', 'zec', 'ltc']
const arr2 = [3,4,5,6,7,8,]

//arr.forEach((element) => console.log(element + ' wanted coin')); 

const mapedArr = arr.map((element) =>  element + ' algorithm')
const fiteredArr = arr2.filter((element) =>  element % 2 === 0);



const str = 'some string, some devil';

const strModified = str.replace('some', 'dome');
console.log(strModified);


